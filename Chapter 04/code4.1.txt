    constructor() {        // Set the initial counter value to zero        counter = 0;    }    // Function to increment the counter value by one    function incrementCounter() public {        counter++;    }    // Function to retrieve the current counter value    function getCounter() public view returns (uint256) {         return counter;    }}// SPDX-License-Identifier: MIT pragma solidity ^0.8.0;// Contract declarationcontract MathContract {    // State variable to store the result    uint256 public result;    // Function to add two numbers and store the result     function addNumbers (uint256 _a, uint256 _b) public {        // Adding the two input numbers and storing the result in the state variable         result = _a + _b;    }    // Function to get the stored result    function getResult() public view returns (uint256) {         // Retrieving and returning the stored result    return result;    }}